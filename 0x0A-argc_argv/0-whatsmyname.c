#include <stdio.h>

/**
 * main - prints a program name
 * @argc: argument count
 * @argv: arguments vector
 * Return: none
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
