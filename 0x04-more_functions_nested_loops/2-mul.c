#include "main.h"

/**
 * mul - multiply 2 integers
 * @a: an argument to be multipied
 * @b: the 2nd argument to be multiplied
 * Return: result of the multiplied values
 */

int mul(int a, int b)

{
	return (a * b);
}
